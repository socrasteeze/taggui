[pytest]
testpaths = tests
# The application imports its own modules as top-level packages (`utils.image`,
# `models.image_list_model`, ...), so `taggui/` has to be on the import path.
pythonpath = taggui
filterwarnings =
    error::DeprecationWarning:utils.*
    error::DeprecationWarning:models.*
